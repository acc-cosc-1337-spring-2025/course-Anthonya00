const double RATE = .5; //can only be changed here

/*
Create double value-return function get_area_of_circle with 
double paramter (radius)
*/

double calculate_interest(int amount)
{
    return amount * RATE;
}